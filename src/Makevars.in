PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CFLAGS)

R_SOURCES = $(wildcard *.c)
OBJECTS = @ADD_OBJS@ $(R_SOURCES:.c=.o)

### Start making here.
all: $(SHLIB)

$(SHLIB): $(OBJECTS)
