PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CFLAGS)

ADD_OBJS = lapack/sblas.f lapack/slapack1.f lapack/slapack2.f lapack/slapack3.f lapack/slapack4.f

$(SHLIB): $(ADD_OBJS)
